#include <stdint.h>
#include <stddef.h>
#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#define ENCRYPTION_KEY 103

// void	decrypt(uint8_t* bytes, uint64_t size, uint8_t key);
void	print_woody(void);

int
main()
{
	print_woody();
	// if (ac != 2)
	// 	return (EXIT_FAILURE);
	
	// char*	data = strdup(av[1]);
	// if (!data)
	// 	return (EXIT_FAILURE);
	
	// size_t	size = strlen(data);
	// decrypt((uint8_t *)data, size, ENCRYPTION_KEY);
	// printf("Encrypted: %s$\n", data);

	// decrypt((uint8_t *)data, size, ENCRYPTION_KEY);
	// printf("Encrypted: %s$\n", data);

	// free(data);
	// return (EXIT_SUCCESS);
}
